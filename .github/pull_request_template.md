<!--
Release label — merging this PR cuts a version from its labels (see RELEASE.md):

  semver:breaking   0.15.19 -> 0.16.0   breaking change (pre-1.0 bumps the minor)
  semver:patch      0.15.19 -> 0.15.20  fix / feature / dependency bump
  release:skip      no version change, no release

No label behaves as semver:patch, so a docs-only or CI-only change still cuts a
release unless you add release:skip.
-->

## What changed

## Why
